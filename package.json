{
  "name": "jaegwan-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:hosting": "npm run build && npx firebase-tools deploy --only hosting",
    "deploy:preview": "npm run build && npx firebase-tools hosting:channel:deploy preview --expires 7d"
  },
  "dependencies": {
    "pdfjs-dist": "^5.4.624",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-pdf": "^10.4.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^5.4.14"
  }
}
